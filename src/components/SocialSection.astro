---
const base = import.meta.env.BASE_URL;

const tiktokUrl = "https://www.tiktok.com/@twoaway";
const instagramUrl = "https://www.instagram.com/p/XXXXXXX/";

// Tegyük az asseteket a public/img mappába
const tiktokImage = `${base}img/twoaway-tiktok-post.jpg`;
const instagramImage = `${base}img/twoaway-instagram-post.jpg`;
---

<section id="social" class="w-full bg-white py-20">
  <div class="container mx-auto space-y-12 text-center">
    <!-- Fejléc -->
    <div class="space-y-4 max-w-2xl mx-auto">
      <p class="inline-flex items-center gap-2 rounded-full bg-sky-50 px-4 py-1 text-xs font-semibold text-sky-700">
        <span class="w-2 h-2 rounded-full bg-sky-500"></span>
        Kövess minket utazós tartalmakért
      </p>
      <h2 class="text-3xl md:text-4xl font-bold text-slate-900">
        TikTokon és Instagramon is velünk utazhatsz
      </h2>
      <p class="text-sm md:text-base text-slate-600 leading-relaxed">
        Valós árak, friss ajánlatok és kulisszák mögötti tartalmak minden nap.
      </p>
    </div>

    <!-- Social kártyák -->
    <div class="grid gap-8 md:grid-cols-2 max-w-5xl mx-auto">
      <!-- TikTok -->
      <a
        href={tiktokUrl}
        target="_blank"
        class="group block rounded-[32px] bg-white border border-slate-200 p-6 shadow-sm hover:shadow-xl hover:-translate-y-1 transition"
      >
        <div class="flex items-center justify-between mb-4">
          <div class="flex items-center gap-3">
            <div class="w-9 h-9 rounded-full bg-slate-100 flex items-center justify-center text-lg">
              🎵
            </div>
            <div class="text-left">
              <p class="text-xs uppercase tracking-wide text-slate-500">TikTok</p>
              <p class="text-sm font-semibold text-slate-900">@twoaway</p>
            </div>
          </div>
          <span class="text-[11px] px-2 py-1 rounded-full bg-slate-100 text-slate-600">
            Short-form • Tippek
          </span>
        </div>

        <div class="rounded-3xl overflow-hidden border border-slate-200 bg-black aspect-square">
          <img
            src={tiktokImage}
            alt="TikTok tartalom"
            class="w-full h-full object-cover group-hover:scale-[1.02] transition"
            loading="lazy"
          />
        </div>

        <div class="mt-4 text-left space-y-1">
          <p class="text-sm font-semibold text-slate-900">
            Last minute city break valós árakon
          </p>
          <p class="text-xs text-slate-500">
            Rövid videó, ahol azonnal láthatod a tényleges költségeket.
          </p>
        </div>
      </a>

      <!-- Instagram -->
      <a
        href={instagramUrl}
        target="_blank"
        class="group block rounded-[32px] bg-white border border-slate-200 p-6 shadow-sm hover:shadow-xl hover:-translate-y-1 transition"
      >
        <div class="flex items-center justify-between mb-4">
          <div class="flex items-center gap-3">
            <div class="w-9 h-9 rounded-full bg-slate-100 flex items-center justify-center text-lg">
              📸
            </div>
            <div class="text-left">
              <p class="text-xs uppercase tracking-wide text-slate-500">Instagram</p>
              <p class="text-sm font-semibold text-slate-900">@twoaway</p>
            </div>
          </div>
          <span class="text-[11px] px-2 py-1 rounded-full bg-slate-100 text-slate-600">
            Carousel • Tippek
          </span>
        </div>

        <div class="rounded-3xl overflow-hidden border border-slate-200 bg-black aspect-square">
          <img
            src={instagramImage}
            alt="Instagram tartalom"
            class="w-full h-full object-cover group-hover:scale-[1.02] transition"
            loading="lazy"
          />
        </div>

        <div class="mt-4 text-left space-y-1">
          <p class="text-sm font-semibold text-slate-900">
            3 napos útiterv egy városnézéshez
          </p>
          <p class="text-xs text-slate-500">
            Menthető carousel hasznos infókkal és ajánlásokkal.
          </p>
        </div>
      </a>
    </div>

    <p class="text-xs md:text-sm text-slate-400 pt-4">
      TikTok: @twoaway • Instagram: @twoaway
    </p>
  </div>
</section>
