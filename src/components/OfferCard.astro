---
const { title, subtitle, image } = Astro.props;
---

<article class="relative overflow-hidden rounded-3xl bg-slate-900 h-full max-h-[700px]">
  <img
    src={image}
    alt={title}
    class="w-full h-full object-cover"
    loading="lazy"
  />
  <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent" />

  <div class="absolute bottom-0 left-0 right-0 p-4 md:p-5">
    <h3 class="font-semibold leading-snug text-sm md:text-base text-white">
      {title}
    </h3>
    {subtitle && (
      <p class="mt-1 text-xs md:text-sm leading-snug text-white/90">
        {subtitle}
      </p>
    )}
  </div>
</article>
